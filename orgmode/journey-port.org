#+TITLE: Journey: port a package
#+OPTIONS: toc:nil num:nil
#+begin_export html
<pre class="mermaid">
flowchart LR
  conda/spack --> eb-stack --> .eb
</pre>
#+end_export
#+begin_export rst
.. figure:: _static/diagrams/journey-port.svg
   :alt: journey port
   :class: eb-diagram

   journey port.
#+end_export
Previous rewrite failed the voice gate: rgoswami.AIVocabulary, rgoswami.TricolonAbuse.
Rewrite again without those hinges.
Keep every fact.
Counts first.
Impersonal.
No drumroll.
TS exploration methods identify a candidate transition mechanism.
The estimate of the transition rate (under HTST) can be calculated from the energy barrier.
The energy barrier is the difference between the energy at the saddle and the reactant state.
The dimer does not fire on Baker-Chan (0/24). On OptBench it fires near a shallow basin. Those 7/59 cases regress. OCI-NEB cut the Baker-Chan force count from 13920 to 5712. There are 0/24 regressions. Every number traces to a Snellius job. The index is in the appendix. Packing jobs does not save SBU. The bill is
