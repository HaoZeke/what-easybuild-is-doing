#+TITLE: Chapter 2. A toolchain is a hierarchy
#+OPTIONS: toc:nil num:nil

#+begin_questions
- What does =foss-2025a= name, if it is not a label?
- Which toolchains may a =foss= package depend on?
- Is =system= in the default hierarchy walk?
#+end_questions

#+begin_objectives
1. Say what a toolchain names, and why it is not a label.
2. Given a package's toolchain, say which other toolchains its
   dependencies may have been built with.
3. Recognise a =_verify_toolchain= failure as belonging to the toolchain
   module rather than to your easyconfig.
#+end_objectives

This line looks like a sticker. It is not.

#+begin_src easyconfig :widget parse :hydrate load
name = 'code-server'
version = '4.130.0'
toolchain = SYSTEM
#+end_src

=SYSTEM= is the simplest value that line accepts, and almost
nothing on a real cluster uses it. What appears instead:

#+begin_src easyconfig :widget hierarchy
toolchain = {'name': 'foss', 'version': '2025a'}
#+end_src

Read as a name, =foss-2025a= looks like a label for "the 2025a
open-source compiler stack". It is the top of a structure. That
structure is which versions of which libraries a package on this
toolchain is allowed to see.

Ask the sample. What that line names is not one toolchain. It is a
list.

* The list has an order, and the order does work

The function is =get_toolchain_hierarchy=, in
=easybuild/framework/easyconfig/easyconfig.py=, and its docstring says
what it produces:

#+begin_quote
Determine list of subtoolchains for specified parent toolchain. Result
starts with the most minimal subtoolchains first, ends with specified
toolchain.
#+end_quote

For the =foss= family the members are =GCCcore=, =GCC=, =gompi=,
=gfbf= and =foss=, each a real toolchain in its own right and each
installed as a real module. Your package declares the last one and gets
to depend on things built with any of them.

The order matters and it is not a straight line. =GCCcore= comes before
=GCC=, which comes before both =gompi= and =gfbf=, which both come
before =foss=. Those middle two are at the same level: =gompi= is =GCC=
plus an MPI, =gfbf= is =GCC= plus the numerical libraries, and neither
is built on the other. So a printed list has to put one of them first,
and which one carries no meaning. Ask the sample and it says so.

A =foss= package can depend on a =GCCcore= build. A =GCCcore=
package cannot depend on a =foss= build. Courtesy has nothing to do
with it. A =GCCcore= package's hierarchy does not contain =foss=, so
a =foss= dependency is never a candidate. The robot then reports that
it cannot find something present right there on disk.

* The shape is a graph

The =foss= line is a chain, which makes it a bad example for
understanding what the code does. EasyBuild's own docstring picks a
harder one, and draws it:

#+begin_example
        goolfc
        /     \
     gompic    golfc(*)
          \    /   \      (*) optional toolchains, not compulsory
          gcccuda golf(*)
              \   /
               GCC
              /  |
      GCCcore(*) |
              \  |
             (system)
#+end_example

Two toolchains are at the same level with a shared ancestor, so the
walk cannot be "follow =SUBTOOLCHAIN= upward until it runs out". A
comment in the implementation states it: the search is breadth-first,
"required to take into account the potential for multiple
subtoolchains".

Note the asterisks. =GCCcore= is *optional*. A toolchain class carries
an =OPTIONAL= attribute, and an optional level counts only when it is
present. So "the hierarchy of =foss-2025a=" is not a property of the
string =foss-2025a=; it is a property of what is installed.

#+ATTR_EB: :id EB-Toolchain-1 :title What may this package depend on
#+begin_exercise
Ask the sample above for =foss-2025a= and read the members back.

Now suppose you are writing an easyconfig whose toolchain is
=GCCcore-14.2.0=.

1. Which of the members it printed could your dependencies have been
   built with?
2. Which could they not?
3. A colleague says "but the =foss= module is right there, I can see it
   in =module avail=". What has gone wrong with that reasoning?

#+begin_solution
Your package is on =GCCcore=, so its hierarchy is =system= then
=GCCcore-14.2.0=, and nothing above. A dependency built with =GCC=,
=gompi=, =gfbf= or =foss= is not a candidate.

The direction is the thing people invert. A =foss= package may depend on
a =GCCcore= build, because =GCCcore= is in the =foss= hierarchy. A
=GCCcore= package may not depend on a =foss= build, because =foss= is not
in the =GCCcore= one.

Your colleague is reading =module avail=, which lists what is installed.
The robot is asking a different question: what is installed *and in this
package's hierarchy*. The module is there. It is not a candidate. The
error says it cannot be found, because those are two different
questions.
#+end_solution
#+end_exercise

* The system toolchain is not always in it

The same docstring then qualifies the bottom of the list:

#+begin_quote
The system toolchain is considered the most minimal subtoolchain only
if the =add_system_to_minimal_toolchains= build option is enabled.
#+end_quote

That option only changes the walk when a dependency did *not* name a
toolchain. =('CUDA', '12.9.1', '', SYSTEM)= still finds the SYSTEM
module, flag or no flag. What the option does not do, off by default,
is treat a bare =('zlib', '1.3.1')= as "also try SYSTEM". A foss
package that forgot the fourth field is looking for
=zlib/1.3.1-foss-…= or a subtoolchain of foss, not =zlib/1.3.1=.

* What a toolchain module has to contain

A toolchain occupies a position in a graph. It is also a module, and
the module has to agree with the class. EasyBuild checks:

#+begin_example
List of toolchain dependency modules and toolchain definition do not match
(found [...] vs expected {...})
#+end_example

That comes from =_verify_toolchain= in
=easybuild/tools/toolchain/toolchain.py=, and it fires in the *prepare*
step, seconds in, before anything is unpacked. Three properties of that
check, together, make it unfixable from your easyconfig:

- It reads the *direct* dependencies of the toolchain module, and only
  those. The call is =dependencies_for(mod_name, ..., depth=0)=. A
  library that arrives transitively does not count.
- It matches on module *name*, so versions and versionsuffixes are
  invisible to it.
- Elements are dropped from the expected set only when they are
  optional and absent, and the base =is_required= returns =True= for
  everything. So a subclass has to opt an element out; nothing opts out
  by accident.

The failure looks like your package's fault and is not. A framework
release that renames or splits a toolchain element breaks *every*
package on that generation at once. No easyconfig parameter reaches the
check.

EasyBuild 5.2 did exactly this. In toolchains built on the NVIDIA HPC
SDK (=NVHPC=), the compiler element became a module named
=nvidia-compilers=. Toolchain modules written before the split still
expected the old name.

The tell is that the two lists in the error differ by one element. Read
the lists, not the package. The difference names the element, and
the fix is a toolchain module that carries it. In practice that means
moving to a generation whose toolchain modules already do.

* The same name can mean two toolchains

=get_toolchain_hierarchy= contains this, in full:

#+begin_quote
Stopgap solution until deprecated compiler-only NVHPC toolchain is
removed. Both the new and old NVHPC toolchains share the same name, we
need to selectively filter one out based on the version of the
toolchain.
#+end_quote

Two different toolchain classes, one string. The framework disambiguates
by version number, with a =TODO: delete with EasyBuild 6.0= beside it.

If =foss-2025a= were a label, that code would be unnecessary.
