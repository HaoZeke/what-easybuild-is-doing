#+TITLE: When it fails
#+OPTIONS: toc:nil num:nil
#+begin_export rst
.. figure:: _static/diagrams/recipe-fail-2.svg
   :alt: a log to a usual cause to the page that names it
   :class: eb-diagram

   When it fails: the log, then the page that already named that cause.
#+end_export
#+begin_export rst
.. figure:: _static/diagrams/recipe-fail.svg
   :alt: log to cause to fix
   :class: eb-diagram

   When it fails: the log, then the page that named that cause.
#+end_export
#+begin_export rst
.. figure:: _static/diagrams/recipe-fail-causes.svg
   :alt: usual fail causes: pin, versionsuffix, isolation, checksum
   :class: eb-diagram

   When it fails, the log names a stage. The usual causes are a pin, a suffix, isolation, or bytes.
#+end_export
Parameters that are misread cause the greatest time consumption.
The log shows that the file is not the problem.
#+begin_export rst
.. toctree::
 :maxdepth: 1

 05-a-versionsuffix-is-part-of-the-name
 06-what-installed-means
 07-a-checksum-is-a-claim-about-bytes
 08-one-easyconfig-many-installs
 09-a-hook-changes-a-file-you-are-not-reading
 20-reading-a-failed-build
 27-four-things-that-actually-happened
 28-your-first-week
#+end_export
