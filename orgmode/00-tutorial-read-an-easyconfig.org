#+TITLE: Tutorial: read an easyconfig the way EasyBuild does
#+OPTIONS: toc:nil num:nil
#+begin_export rst
.. figure:: _static/diagrams/00-tutorial-read-an-easyconfig-2.svg
   :alt: name, version, toolchain, sources on one rail
   :class: eb-diagram

   The tutorial reads four fields. Templates and the easyblock come after the file runs.
#+end_export
#+begin_export rst
.. figure:: _static/diagrams/00-says-vs-means.svg
   :alt: file text through the namespace to the model
   :class: eb-diagram

   The tutorial object is the model, not the command. The file is evaluated.
#+end_export
This tutorial evaluates a file the way EasyBuild does.
Templates are formatted after the file runs.
The easyblock is derived from the name.
The toolchain decides which dependencies are visible.
The robot resolves a bare filename.
Each sample on this page is editable and runs in the browser.
Recordings later in the book are labelled as recordings and are not typed.
The expected output is under the sample.
Twenty minutes.
Nothing to install.
Nothing to put back.
*Who this is for.* You can read code.
You have met an easyconfig, or you are about to.
You do not need EasyBuild, a cluster, an account, or Python.
You will not need them here.
#+begin_questions
- Why does =SYSTEM= have no quotes?
- What does =%(version)s= become, and when?
- How does EasyBuild pick an easyblock when the file never names one?
#+end_questions

#+begin_objectives
1. Say what an easyconfig means, as against what it says.
2. Predict which file EasyBuild will download from a version number.
3. Name the easyblock EasyBuild will look for, for any software name.
4. Say which dependencies a toolchain lets a package see.
#+end_objectives

* Before you start
Check that JavaScript is enabled in the browser.
Each sample below runs on this page on the user's machine.
A live sample differs from a printed one.
A live sample shows *editable, runs as you type* above it and provides an answer below before any interaction.
When a sample indicates that the engine is not loaded, JavaScript is disabled or still loading; wait briefly and then reload.
* Step 1: what the file says, and what it means
A real easyconfig is provided.
The easyconfig installs a browser‑based VS Code on a supercomputer.
The easyconfig should be read first.
The answer underneath it should be read next.
#+begin_src easyconfig :widget parse :hydrate load
name = 'code-server'
version = '4.130.0'

homepage = 'https://github.com/coder/code-server'
description = """Run VS Code on any machine anywhere and access it in the browser."""

toolchain = SYSTEM

source_urls = ['https://github.com/coder/code-server/releases/download/v%(version)s/']
sources = ['code-server-%(version)s-linux-%(mapped_arch)s.tar.gz']

moduleclass = 'tools'
#+end_src
The evaluated model includes three items missing from the file above.
=toolchain= reads =({name: system, version: system})= rather than the word =SYSTEM=.
EasyBuild places that name in scope before evaluating the file, using the constant ={'name': 'system', 'version': 'system'}=.
It is a variable and =moduleclass= adjacent to it is a string.
=source_urls= is a list of URL prefixes. After the file runs, EasyBuild
formats =%(version)s= in =sources= to =4.130.0=.
The file itself stored the literal value.
File order is irrelevant; =sources= may appear before =version=.
The final line reports an easyblock that the file never mentions.
* Step 2: change the version, and watch what follows
=4.130.0= becomes =4.131.0=.
=code-server= becomes =codeserver=.
=EB_code_minus_server= becomes =EB_codeserver=.
* Step 3: break it on purpose
Delete the =version= line.
*Expected output.* The model names the missing field and stops.
It does not guess.
It does not carry on with a blank.
Now put =version= back and delete the =sources= line instead.
*Expected output.* The model comes back without =sources=, and no complaint.
A missing =version= stops everything.
A missing =sources= is a file that downloads nothing, which is a legal easyconfig and a useless one.
One more.
Add this line at the bottom:
#+begin_example
checksums = [
#+end_example
*Expected output.* =checksums= show as *not modelled*.
The page's reader handles only single-line assignments.
The list shows omitted items rather than fabricated ones.
A multi-line construct needs the actual parser.
The parser runs in Python.
The complete engine is a full program.
The engine is not the short snippet displayed here.
* Step 4: which easyblock will EasyBuild look for
An easyconfig without an =easyblock= assumes a software-specific one exists.
EasyBuild derives the name by converting the software's name one character at a time.
#+begin_src easyconfig :widget easyblock :hydrate load
code-server
c++
Xerces-C++
Python-bundle-PyPI
GROMACS
#+end_src
*Expected output.* punctuation spelled out as words: the hyphen in =code-server= becomes =minus=, and =c++= becomes =EB_c_plus__plus_=.
Look at the doubled separator in the middle.
The substitution runs per character and collapses nothing, and that is what keeps it reversible.
Now type a name with a low line in it, say =my_tool=.
*Expected output.* =_underscore_= .
That character is encoded too, and it has to be.
Left alone, =_minus_= could mean either an encoded hyphen or a name that really contained the letters =minus=.
Two things follow.
Given a name, you know which file EasyBuild will try to import.
And given a class name in a traceback, you can read it backwards to the software.
* Step 5: what a toolchain lets you see
A dependency qualifies as a candidate only when built with a toolchain present in the package's hierarchy.
Determine the contents of a hierarchy.
#+begin_src easyconfig :widget hierarchy :hydrate load
toolchain = {'name': 'foss', 'version': '2025a'}
#+end_src
The widget lists the hierarchy. Start at =system=. The last name is
=foss-2025a=. Two members of that list are toolchains a package on
=foss-2025a= may depend on.
A package on =foss-2025a= may depend on any component built with any member of that list.
A package on =GCCcore= cannot depend on a =foss= build.
The rule is not a matter of politeness; the dependency is not a candidate, causing the robot to report that it cannot locate something visible on disk.
Change =2025a= to =2024a=, then to =1999z=.
Provide a different chain, followed by a refusal indicating which generations the page knows.
A hierarchy reflects what is installed rather than the string.
* Where to go next
- The next page in RecipeReader: [[file:01-an-easyconfig-is-python.org][An easyconfig is Python]].
- A build failed: [[file:journey-a-build-failed.org][Journey: a build failed]].

#+begin_keypoints
- The file is Python. EasyBuild evaluates it in a prepared namespace.
- A missing =version= stops the parse. A missing =sources= line is legal and useless.
- A missing =easyblock= line is still an easyblock: the name is transformed.
- A toolchain is the top of a hierarchy. That hierarchy is the set of legal dependencies.
#+end_keypoints
