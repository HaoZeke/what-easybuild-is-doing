#+TITLE: Inspect writes a work queue, not a recipe
#+OPTIONS: toc:nil num:nil
#+begin_export html
<pre class="mermaid">
flowchart LR
  foreign --> residuals --> plan
</pre>
#+end_export
#+begin_export rst
.. figure:: _static/diagrams/33-inspect-residuals-and-the-plan.svg
   :alt: inspect
   :class: eb-diagram

   inspect.
#+end_export

#+begin_questions
- What two files does =package inspect= write, and which claim do they
 establish?
- When is a leftover a residual, and when is it only a parser note?
- Why does =package plan= exit without =--stack-policy=?
#+end_questions

#+begin_objectives
1. Run =package inspect= on a foreign recipe and name every section of
 =package.plan.json= that is evidence, and every residual that is work.
2. Tell a =mechanical= residual from a =judgment= residual from a
 =blocking= one, and say which stage each belongs to.
3. Write the =package plan= command that turns that inspect tree into a
 lock and a =.eb=, including the stack policy the CLI requires.
#+end_objectives

#+begin_prerequisites
- [[file:12-eb-stack-updater-and-generator.org][eb-stack ports a recipe and bumps one]]: the
 claim ladder. This chapter is the first two rungs' artifacts.
#+end_prerequisites
The link [[file:12-eb-stack-updater-and-generator.org][eb-stack ports a recipe and bumps one]] placed the conda-forge recipe beside the =.eb= and halted at the residual queue.
=package inspect= is the command executable from a fresh clone that lacks EasyBuild and a robot tree.
Its output is the remainder of this chapter.
The fixture comprises the rattler-build v1 recipe for eOn, captured from the conda-forge feedstock.
#+begin_example
eb-stack package inspect \
 --source fixtures/foreign_ingest/conda_eon/recipe.yaml \
 --format conda-forge \
 --toolchain-name foss \
 --toolchain-version 2026.1 \
 --package-config examples/packages/common.toml \
 --package-config examples/packages/eon.toml \
 --out-dir /tmp/eon-inspect
#+end_example

Two files come out, and nothing else:

#+begin_example
/tmp/eon-inspect/package.plan.json
/tmp/eon-inspect/package.sbom.cdx.json
#+end_example
The =locks/= directory is missing.
The =easyconfigs/= directory is missing.
Resolvo is not called by Inspection.
No rung of the ladder is established.
* The plan is a manifest with provenance
=package.plan.json= has =schema_version= 1.
Unknown fields are rejected.
The sections that matter on a first read are listed below.
=origin= records =conda-forge=, =spack=, =pypi=, =cran=, =cargo=, or =easy-build=.
=package= records normalised name, version, homepage, license.
=sources= records URLs, filenames, SHA-256, VCS identity.
=dependencies= records names, constraints, roles, conditions, provenance.
=build= records toolchain, easyblock, patches, typed easyconfig parameters.
=profiles= records independently installable products.
=outputs= records requested profile and stack-policy pairs.
=residuals= records the work queue.
Provenance on a source or a dependency is =exact=, =derived=, or =ambiguous=, plus a span.
Written paths are identities such as =conda-forge/recipe.yaml=, =spack/package.py=.
Written paths are not checkout locations.
Two hosts produce the same JSON from the same input.
A conda =build.number= is a parser *note*.
A compiler or =cross-python= template that the EasyBuild toolchain already represents is also a parser *note*.
Notes are diagnostics.
Planning never greps them for the word "residual".
Anything that needs a human or a later stage is a typed residual instead.
* Residuals are the work queue
* Residual
=id= R1 =stage= analysis =category= method_identification =severity= info =summary= TS exploration methods identify a candidate transition mechanism.

* Residual
=id= R2 =stage= analysis =category= transition_rate_estimation =severity= info =summary= The estimate of the transition rate under HTST can be calculated from the energy barrier.

* Residual
=id= R3 =stage= analysis =category= energy_barrier_definition =severity= info =summary= The energy barrier is the difference between the energy at the saddle and the reactant state.

* Residual
=id= R4 =stage= testing =category= dimer_behavior =severity= info =summary= The dimer does not fire on Baker-Chan (0/24).

* Residual
=id= R5 =stage= testing =category= dimer_behavior =severity= info =summary= On OptBench the dimer fires near a shallow basin and those 7/59 cases regress.

* Residual
=id= R6 =stage= optimization =category= force_count_reduction =severity= info =summary= OCI-NEB cut the Baker-Chan force count from 13920 to 5712, with 0/24 regressions.

* Residual
=id= R7 =stage= provenance =category= job_tracing =severity= info =summary= Every number traces to a Snellius job.

* Residual
=id= R8 =stage= provenance =category= appendix_reference =severity= info =summary= The index is in the appendix.

* Residual
#+begin_example
{
 "id": "foreign:imperative-patch:0",
 "stage": "parse",
 "category": "imperative-patch",
 "severity": "judgment",
 "summary": "imperative patch method requires EasyBuild translation",
 "provenance": {
 "span": {"path": "package.py", "start_line": 42}
 }
}
#+end_example
Severity has three values for triage.
- =mechanical=: deterministic formatting or metadata.  
- =recipe format=: can close some of these.  
- =judgment=: package-authoring or target evidence is required.  
- A Spack =def patch(self):= method that is not a file is this.  
- =blocking=: the next pipeline stage cannot make a valid claim.  
- A missing SHA-256 on a version bump without =--source-checksum= is this.  
Stage is =parse=, =normalization=, =resolution=, =emission=, =build=, or =verification=.
An inspect residual is almost always =parse=.
A bump that changes the package version and does not pass =--source-checksum= adds a residual.
The foreign-ingest README is explicit about the limit.
These fixtures drive parser regression.
They do not claim parity with a hand-authored EasyBuild pull request.
Product flags, generation pins, and multi-source extract layout remain residual.
That sentence explains why inspect exists as a command of its own.
The work queue is visible *before* Resolvo spends a robot tree on a recipe that still needs judgment.
The planned SBOM, =package.sbom.cdx.json=, is CycloneDX 1.5 derived from the same plan.
It is a pre-build inventory.
It carries no generation timestamp.
Two inspect runs of the same input hash the same.
It does not establish =builds= or =binary-verified=.
#+ATTR_EB: :id EB-Residual-1 :title Note, residual, or claim
#+begin_exercise
Four inspect outputs. For each, say whether it is a parser note, a
typed residual (and which severity), or a claim.

1. A conda =build.number= of 3.
2. A Spack =def patch(self):= that applies a sed at build time.
3. A source URL with a SHA-256 taken from the feedstock.
4. =package.sbom.cdx.json= listing the same source.

#+begin_solution
1. A parser note. The EasyBuild toolchain represents the rebuild
 counter; nothing in the next stage has to act on it.

2. A =judgment= residual, category =imperative-patch=, stage
 =parse=. There is no patch file to copy beside the emitted
 =.eb=. Someone has to write one, or drop the change.

3. Evidence in =sources=. Not a residual. Provenance is =exact=.

4. The planned SBOM. Not a claim. The same inventory in a
 different schema. =resolves= still has not been established.
#+end_solution
#+end_exercise

* Plan is inspect plus Resolvo, and it needs a policy
The command defining =resolves= is =package plan=.
The current CLI requires =--stack-policy=. eb-stack ports a recipe and bumps one's example left it out.
The live form from the tutorial is:
#+begin_example
eb-stack package plan \
 --source fixtures/foreign_ingest/conda_eon/recipe.yaml \
 --format conda-forge \
 --toolchain-name foss \
 --toolchain-version 2026.1 \
 --package-config examples/packages/common.toml \
 --package-config examples/packages/eon.toml \
 --easyconfigs "$ROBOT" \
 --easyconfigs /tmp/eb-stack/overlay \
 --stack-policy examples/stacks/eon-foss-2026.1.toml \
 --out-dir "$BUNDLE"
#+end_example

The generated tree is now:

#+begin_example
package.plan.json
package.sbom.cdx.json
locks/default.lock.json
easyconfigs/e/eOn/eOn-2.16.0-foss-2026.1.eb
#+end_example
The lock corresponds to the Resolvo result.
Direct dependencies specify selected versions, toolchains, versionsuffixes, source easyconfig paths, build/runtime roles, pin outcomes, and candidate exclusions.
The stack-policy TOML is mandatory.
Optional decoration is not allowed.
The eOn example records the Eigen 5 identity required by the safemath core-guard patch as a =preferred= pin with the complete EasyBuild toolchain and versionsuffix.
Resolvo attempts that candidate first.
Another declared requirement may render it infeasible.
The solver selects a compatible alternative and sets =fallback = true= with a reason.
=locked= is the mode that must instead fail.
=package bump= of an existing =.eb= produces the same bundle.
The bump solver applies an unconstrained policy for the requested toolchain.
This occurs without =--stack-policy=.
=plan= of a foreign recipe does not receive that relaxation.
The flag is required.
A direct dependency may lack a robot candidate.
=--package-catalog= and =--package-sources= close the gap without inventing versions.
A closed plan writes companions under =packages/=, a shared =easyconfigs/= overlay, =build-order.json=, and =closure.sbom.cdx.json=.
Robot candidates continue to take precedence over catalog entries.
#+ATTR_EB: :id EB-Plan-1 :title What is missing from this command
#+begin_exercise
A colleague pastes eb-stack ports a recipe and bumps one's plan sample:

#+begin_example
eb-stack package plan \
 --source recipe.yaml \
 --format conda-forge \
 --toolchain-name foss \
 --toolchain-version 2024a \
 --easyconfigs /path/to/easybuild-easyconfigs \
 --out-dir /tmp/new-package
#+end_example
The current CLI processes the provided input and updates the relevant state.
A new file is added; the =preferred= pin in it controls the selection of the default version.
After a successful plan, both the =build_dir= and the =install_dir= paths must exist before =campaign run= starts.
#+begin_solution
1. It exits with an error. =--stack-policy= is required on =package plan=.

2. A stack-policy TOML, for example
 =examples/stacks/eon-foss-2026.1.toml=. A =preferred= pin names
 the candidate Resolvo tries first and records =fallback = true=
 if it cannot keep it. It is not a lock until the solve writes
 =locks/default.lock.json=.

3. At least one =locks/<profile>.lock.json= and one emitted
 =.eb=. Inspect's two JSON files are not enough. The campaign
 reference says so: a campaign requires at least one lock and
 recipe.
#+end_solution
#+end_exercise

#+ATTR_EB: :id sources-33 :title Where this chapter comes from :kind sources
#+begin_detail
eb-stack source and docs, HaoZeke/eb-stack, read 2026-09-11:

- =docs/orgmode/tutorial.org= (inspect then plan, Eigen preferred
 pin, =--stack-policy=).
- =docs/orgmode/reference/cli.org= and
 =docs/orgmode/reference/package-bundles.org= (bundle layout,
 residual severity, planned SBOM, closed catalog bundles).
- =src/main.rs= =PackageCommand::Inspect= / =Plan= / =Bump=.
- =fixtures/foreign_ingest/README.md=.
#+end_detail

#+begin_keypoints
- =package inspect= writes =package.plan.json= and a planned
 CycloneDX SBOM. It establishes no claim.
- Residuals are the work queue. Notes are not. Severity is
 =mechanical=, =judgment=, or =blocking=.
- =package plan= requires =--stack-policy=. The lock and the
 emitted =.eb= are what establish =resolves=.
#+end_keypoints
