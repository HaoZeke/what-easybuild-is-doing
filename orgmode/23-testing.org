#+TITLE: Testing, and the four things it can mean

#+OPTIONS: toc:nil num:nil
#+begin_export rst
.. figure:: _static/diagrams/23-testing-2.svg
   :alt: four distinct questions: suite, sanity, report, ReFrame
   :class: eb-diagram

   Four things are called testing. None of them substitutes for another.
#+end_export
#+begin_export rst
.. figure:: _static/diagrams/23-testing.svg
   :alt: four kinds of testing, not one pipeline
   :class: eb-diagram

   Testing names four questions. A green suite is not a sanity check, a report, or a ReFrame run.
#+end_export
#+begin_export rst
.. figure:: _static/diagrams/23-four-test-meanings.svg
   :alt: smoke, sanity, unit, and ReFrame are four different objects
   :class: eb-diagram

   Testing names four objects. Only ReFrame is a class that runs on a partition.
#+end_export

#+begin_objectives
1. Tell apart the four things called "testing" in an EasyBuild workflow,
 and say which one catches which failure.
2. Run a package's own test suite from an easyconfig, and decide what to
 do when it fails.
3. Produce a test report, and say why upstream requires one from a human.
#+end_objectives

#+begin_prerequisites
- [[file:06-what-installed-means.org][What "installed" means]], for the sanity check.
- [[file:13-how-a-build-executes.org][How a build executes]], for where the =test= and
 =testcases= steps run.
#+end_prerequisites
The test comprises four distinct questions.
Answering the incorrect question results in a broken module being shipped.
They occur in the following order:
1. The package's own test suite runs during the build.  
This is the =test= step.
2. EasyBuild's sanity check runs after the install.  
Refer to What "installed" means.
3. An uploaded test report states that a build succeeded on a real machine.  
4. A portable benchmark or correctness run is performed later on relevant hardware.  
None of these steps subsumes another.
A green test suite indicates that the code computes as intended.
The sanity check indicates that the installation is usable.
A test report indicates that a user's machine successfully executed the build.
A portable test indicates that the software still works on the node used by users.
* The package's own tests
Two parameters exist and they are not the same.
=runtest= drives the =test= step, which runs *before* install.
On the base =EasyBlock= it is a whole command.
Under =ConfigureMake=, which is what most easyconfigs use, it is appended to =make=, so =runtest = 'test'= means =make test=, and the composed command is =pretestopts + test_cmd + runtest + testopts= with =test_cmd= defaulting to =make=.
That difference catches people: the same value means a command in one easyblock and an argument in another.
=tests= is a different parameter and a different step.
It is a list of scripts run in the =testcases= step, at the very end, after the module exists, and a script fails by returning non-zero.
Three ways exist for a failing test step to proceed.
=--ignore-test-failure= lets the build continue and records the failure.
The framework wraps the test step precisely so this is possible: a failure is routed to =report_test_failure= rather than aborting.
=--skip-test-step= does not run it at all.
=skipsteps = ['test']= in the easyconfig does the same thing permanently, for everyone, and that is the one to think twice about.
[[file:27-four-things-that-actually-happened.org][Four things that actually happened]]'s third case is a =skipsteps= that was defensible and still wrong: the check was reporting a real defect somewhere else, and switching it off moved the discovery to a user.
*a skipped test needs a comment saying why.* *an ignored test needs somebody to have read the failure.* Both are legitimate.
* The sanity check is a test
Restating this is important.
The =sanity_check_paths= assert that files and directories exist.
The =sanity_check_commands= runs commands.
The check *executes the software*.
That is [[file:06-what-installed-means.org][What "installed" means]].
The only addition is interaction with the options above.
The =--skip-sanity-check= exists.
Under =--skip= the sanity check is deliberately *not* skipped.
One misconception is that it leads people to disable the RPATH check on GPU packages.
=libcuda.so.1= lives with the driver.
=libcuda.so.1= exists only on a node that has a GPU.
EasyBuild knows that it exempts =libcuda.so=, =libcuda.so.1=, =libnvidia-ml.so=, and =libnvidia-ml.so.1= by default.
Anything else reported by the check is a real finding.
* A test report
Upstream requirements for merging an easyconfig list CI capabilities and limits.
The pull request targets =develop=.
CI status is green.
*a successful test report has been submitted*.
A maintainer approved the pull request.
The author did not merge their own change.
The third condition is present.
Upstream CI parses each easyconfig.
Upstream CI style‑checks each easyconfig.
Upstream CI verifies that checksums are present.
Upstream CI checks for an easyconfig for every dependency.
Upstream CI checks that no dependency graph contains a version conflict.
The contributed package is not built.
Human verification provides evidence that a build works.
The process produces the evidence.
#+begin_example
eb --from-pr <PR#> --upload-test-report --force --robot
#+end_example
=--upload-test-report= posts the report as a GitHub gist.
=--upload-test-report= requires a token with =gist= permission.
=--upload-test-report= adds a summary comment to the pull request.

=--dump-test-report= writes the report locally.

Both options imply =--keep-going=.
A run that generates a report continues past the first failure.
A report covering nine of ten easyconfigs is more useful than one covering only one.
#+ATTR_EB: :id test-report-contents :title What a test report contains :kind reference
#+begin_detail
Every report carries:

- the easyconfigs processed, and the result for each;
- the time and date;
- the exact =eb= command line;
- the full EasyBuild configuration in effect;
- system information: hostname, operating system, architecture;
- the modules loaded at the time;
- the full environment of the session.

For each easyconfig that *failed*, a partial log is uploaded as its own
gist and linked from the report.

That last item in the first list is why =--test-report-env-filter=
exists: a regular expression whose *matching* variable names are
excluded. The documented value to start from is

#+begin_example
export EASYBUILD_TEST_REPORT_ENV_FILTER='^SSH|USER|HOSTNAME|UID|.*COOKIE.*'
#+end_example
A report is generated regardless of request.
The report is placed beside the installation log as =<log basename>_test_report.md=.
Read a report before uploading the first one.
#+end_detail
The contents (see [[dive:test-report-contents]]) enumerate the machine, the configuration, and the modules.
A test report is evidence.
A reviewer can tell whether the success applies.
* Testing on hardware the site does not have
The fourth kind.
A build that works on the machine that built it tells nothing about the machine the users are on, which is [[file:27-four-things-that-actually-happened.org][Four things that actually happened]]'s login-node lesson in general form.
EESSI's answer is a portable test suite, =EESSI/test-suite=, written with ReFrame.
Tests are mapped to software in [[https://github.com/EESSI/software-layer-scripts/blob/main/tests/eessi_test_mapping/software_to_tests.yml][tests/eessi_test_mapping/software_to_tests.yml]].
Adding software declares which tests should now run rather than leaving somebody to remember.
The build bot runs them through [[https://github.com/EESSI/software-layer-scripts/blob/main/bot/test.sh][bot/test.sh]] on the architecture the build targeted.
The rule is small: *a test has a location, and a result without its location is not a result.* A green test on a login node and a green test on the GPU node a user submitted to are two different facts, and only one of them answers the ticket.
[[file:part-a-test.org][ReframeReader]] is that kind in full: [[file:39-a-reframe-test-is-a-class.org][A ReFrame test is a class]] through [[file:47-reading-a-failed-reframe-run.org][Reading a failed ReFrame run]].
A test class, the six-stage pipeline, the configuration file, a performance number, then the portable suite and a failed run.
CSCS teaches it as a webinar separate from getting started; EESSI walks a job script, a site-specific test, then a mixin.
The mapping file above is [[file:46-software-maps-to-tests.org][Software maps to tests]].
#+ATTR_EB: :id EB-Testing-1 :title Four reports, four responses
#+begin_exercise
1. A package's =make test= fails on two of 340 tests, in the
 floating-point comparison of an iterative solver.
2. The sanity check fails on =lib/libthing.so=, and the install
 directory contains =lib64/libthing.so=.
3. A colleague's pull request has green CI and no test report. They ask
 you to merge it.
4. Your build passes everything on the build host. A user reports
 =Illegal instruction= on an older node.

For each: which of the four kinds of testing is involved, and what do you
do?

#+begin_solution
1. The package's own test suite. Read the two failures before deciding
 anything: an iterative solver disagreeing in the last digits across
 compilers is common and often acceptable, and a wrong answer is not.
 If they are tolerable, =--ignore-test-failure= for this build and a
 comment in the easyconfig saying which two and why. Not
 =skipsteps=, which would hide the other 338.

2. The sanity check, and it is correct. The library is in =lib64=, so
 either the check should say =lib64= or the build should be told to use
 =lib=. Prefer the second where the build system supports it
 stack where half the packages use =lib= and half =lib64= is a stack
 where every consumer easyconfig has to know which.

3. Neither of you can merge it: upstream requires a test report and
 forbids the author merging. What you *can* do is produce the report,
 with =eb --from-pr <PR#> --upload-test-report --force --robot=, which
 is the useful half of a review anyway. Then a maintainer merges.

4. The fourth kind, and no amount of the first three would have caught
 it. The binary was compiled for the build host's microarchitecture, so
 this is [[file:16-what-a-toolchain-injects.org][What a toolchain injects]]'s
 =optarch=, and the fix is at the site level: build per architecture,
 or set a baseline every node can run. Testing on the build host was
 the mistake, not the build.
#+end_solution
#+end_exercise

#+ATTR_EB: :id sources-23 :title Where this page comes from :kind sources
#+begin_detail
Upstream documentation, fetched 2026-09-10:

- [[https://docs.easybuild.io/unit-tests/][unit tests]]: the three suites, and the statement that they do not build the supported software
- [[https://docs.easybuild.io/contributing/][contributing]]: the merge requirements, and the review process
- [[https://docs.easybuild.io/integration-with-github/][GitHub integration]]: =--upload-test-report=, its gist permission, and =--test-report-env-filter=
- [[https://docs.easybuild.io/version-specific/easyconfig-parameters/][the parameter reference]]: =runtest= against =tests=
- [[https://docs.easybuild.io/version-specific/eb-help/][the option table]]: =--ignore-test-failure=, =--skip-test-step=, =--keep-going=

Code, from EasyBuild 5.3.1: =EasyBlock._test_step= and
=report_test_failure= in [[https://github.com/easybuilders/easybuild-framework/blob/develop/easybuild/framework/easyblock.py][easybuild/framework/easyblock.py]] for how a
failure is routed; =ConfigureMake.test_step= for how =runtest= composes
with =make=; and the RPATH check's default exemptions for the driver
libraries.

The portable-test material is EESSI's:
[[https://github.com/EESSI/test-suite][EESSI/test-suite]] and
[[https://github.com/EESSI/software-layer-scripts/blob/main/tests/eessi_test_mapping/software_to_tests.yml][tests/eessi_test_mapping/software_to_tests.yml]].
#+end_detail

#+begin_keypoints
- Four different things are called testing: the package's own suite, the
 sanity check, a test report, and a portable run on hardware that matters
 about. None of them subsumes another.
- =runtest= is a whole command on the base class and an argument to =make=
 under =ConfigureMake=.
- =--ignore-test-failure= records a failure, =--skip-test-step= avoids it,
 and =skipsteps= removes it for everyone.
- The RPATH check already exempts =libcuda.so.1= and the other driver
 libraries, so anything else it reports is real.
- Upstream CI never builds an easyconfig, which is why the evidence is a
 human's test report and why an author cannot merge their own.
#+end_keypoints
